[project]
name = "agentphone-backend"
version = "0.2.0"
description = "Persistent Android emulator remote control backend with PWA dashboard"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pydantic>=2.7",
  "python-multipart>=0.0.9",
  "aiortc>=1.9",
  "av>=12",
  "pillow>=11",
  "numpy>=2",
]

[tool.uv]
package = false
